<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>EntwineAgents.Intake</RootNamespace>
    <Description>Intake utilities for .NET agents: tolerant XLSX/CSV/OCR-text table reading, a table-shape classifier (which schema is this, which column is which — deterministic first, a validated model residue second), date normalisation, and session-local pseudonymisation (PseudonymMap).</Description>
    <PackageTags>csv;xlsx;ocr;etl;schema-mapping;pseudonymization;llm;dotnet</PackageTags>
  </PropertyGroup>

  <!--
    Intake utilities: spreadsheet/CSV/OCR-text table reading with tolerant header detection, date
    normalisation, and PseudonymMap - typed, stable, session-local pseudonymisation for LLM calls.
  -->
  <ItemGroup>
    <PackageReference Include="ClosedXML" Version="0.104.2" />
  </ItemGroup>

</Project>
